# R
.Rproj.user/
.Rhistory
.RData
.Rdata
.Ruserdata
.Rapp.history
*.Rproj

# Shiny / deploy
rsconnect/

# R package build artifacts
*.tar.gz
*.Rcheck/

# Scratch / smoke-test files
_smoke_tmp.R
*_tmp.R
Rplots.pdf

# Local agent/editor settings (also covered by some global ignores, but a
# repo-level rule is what protects a `git add -A` during a release).
.claude/

# OS
.DS_Store
Thumbs.db
