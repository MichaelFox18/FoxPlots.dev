% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_reshape.R
\name{do_transpose}
\alias{do_transpose}
\title{Transpose a table -- swap rows and columns}
\usage{
do_transpose(data, names_from = NULL, id_col = "name")
}
\arguments{
\item{data}{A data frame.}

\item{names_from}{Optional single column whose values become the new column
headers. NULL (default) generates V1, V2, ... headers from the row order.}

\item{id_col}{Name for the new column holding the original column names.}
}
\value{
A tibble, transposed.
}
\description{
JMP Tables > Transpose. Implemented as a pivot round-trip (keeps types saner
than t(), which forces a matrix). One column can supply the new column
headers; the remaining columns are transposed into rows. When the transposed
columns are of mixed type the values become character (the honest result of
a true transpose).
}
\examples{
do_transpose(data.frame(metric = c("h", "w"), a = c(1, 10), b = c(2, 20)),
             names_from = "metric")
}
