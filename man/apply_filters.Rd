% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_filter.R
\name{apply_filters}
\alias{apply_filters}
\title{Keep the rows of `df` matching every condition (AND).}
\usage{
apply_filters(df, conditions)
}
\arguments{
\item{df}{A data frame.}

\item{conditions}{A list of conditions, each list(col, op, value). An empty
or NULL list returns `df` unchanged.}
}
\value{
`df` with non-matching rows removed.
}
\description{
Keep the rows of `df` matching every condition (AND).
}
\examples{
apply_filters(data.frame(team = c("LAL", "BOS"), pts = c(25, 18)),
              list(list(col = "pts", op = ">", value = 20)))
}
