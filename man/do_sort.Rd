% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_reshape.R
\name{do_sort}
\alias{do_sort}
\title{Sort rows by one or more columns (tall stays tall)}
\usage{
do_sort(data, cols, desc = FALSE)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Character vector of columns to sort by, highest priority first.}

\item{desc}{Logical, recycled to length(cols): TRUE = descending for the
column in the matching position. Default FALSE (all ascending).}
}
\value{
A tibble with the rows reordered.
}
\description{
JMP Tables > Sort. Reorders rows by the given columns in priority order,
each independently ascending or descending. Missing values sort last.
}
\examples{
do_sort(data.frame(g = c("a", "a", "b"), v = c(1, 2, 1)),
        c("g", "v"), desc = c(FALSE, TRUE))
}
