% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_report.R
\name{render_report}
\alias{render_report}
\title{Render a report spec to a file. HTML = a self-contained .html (charts as
base64 data URIs); Word = an editable .docx via officer (charts as embedded
PNGs). Both rasterize the charts and, if a model is present, the regression
diagnostics.}
\usage{
render_report(
  spec,
  file,
  format = c("html", "docx"),
  width = 7,
  height = 4.5,
  dpi = 110
)
}
\arguments{
\item{spec}{A list from report_spec().}

\item{file}{Output path.}

\item{format}{"html" or "docx".}

\item{width, height, dpi}{Per-chart image size.}
}
\value{
`file`, invisibly.
}
\description{
Render a report spec to a file. HTML = a self-contained .html (charts as
base64 data URIs); Word = an editable .docx via officer (charts as embedded
PNGs). Both rasterize the charts and, if a model is present, the regression
diagnostics.
}
