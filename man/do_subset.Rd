% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_reshape.R
\name{do_subset}
\alias{do_subset}
\title{Subset rows and/or columns, with optional (stratified) random sampling}
\usage{
do_subset(
  data,
  cols = NULL,
  sample = c("all", "n", "prop"),
  size = NULL,
  stratify_by = NULL,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Character vector of columns to keep, or NULL (default) for all.}

\item{sample}{One of "all" (no row sampling), "n" (a fixed number of rows),
or "prop" (a fraction of rows).}

\item{size}{For sample = "n", the number of rows; for "prop", a fraction in
(0, 1]. Ignored when sample = "all".}

\item{stratify_by}{Optional column(s) to sample within (stratified sampling).}

\item{seed}{Optional integer RNG seed for a reproducible sample.}
}
\value{
A tibble.
}
\description{
JMP Tables > Subset. Keeps a chosen set of columns and/or a sample of rows.
Sampling can be a fixed count or a proportion, and can be stratified so the
sample is drawn within each group of `stratify_by`.
}
\examples{
do_subset(data.frame(g = rep(c("a", "b"), each = 5), x = 1:10),
          sample = "prop", size = 0.5, stratify_by = "g", seed = 1)
}
