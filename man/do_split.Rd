% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_reshape.R
\name{do_split}
\alias{do_split}
\title{Split one column into many (tall -> wide)}
\usage{
do_split(data, value_col, split_by, group_cols = NULL, values_fn = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{value_col}{Name of the column holding the values to spread
(JMP "Split Columns").}

\item{split_by}{Name of the column whose distinct values become the new
column headers (JMP "Split By").}

\item{group_cols}{Character vector of columns identifying a row, kept as-is
(JMP "Group"). If NULL (default), every column except value_col and
split_by is used as the grouping identity.}

\item{values_fn}{Optional function to resolve duplicate group/split-by
combinations (e.g. mean). The default (NULL) is pivot_wider's behaviour:
duplicates produce list-columns and a warning, so expose this in the UI.}
}
\value{
A tibble in wide form.
}
\description{
JMP Tables > Split. Wraps tidyr::pivot_wider: spreads a value column across
new columns keyed by a "split by" column, keeping the group columns as the
row identity. The inverse of do_stack().
}
\examples{
long <- data.frame(id = c(1, 1, 2, 2), k = c("a", "b", "a", "b"),
                   v = c(5, 3, 2, 4))
do_split(long, value_col = "v", split_by = "k", group_cols = "id")
}
